# End-to-End Tests

Reserved for full-stack flows exercising both frontend and backend
together (e.g., via Playwright). Empty in Phase 0 — the frontend doesn't
exist yet (arrives in Phase 0.2). The first e2e test is expected once
Phase 1 (auth) has both a backend endpoint and a frontend login screen
to drive.
