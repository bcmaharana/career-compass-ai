[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers
markers =
    unit: fast, no infrastructure required
    integration: requires the local Postgres/Redis test infrastructure
    e2e: full-stack, reserved for when the frontend exists
