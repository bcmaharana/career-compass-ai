# Copy to .env.local for local development.
# Empty by default — same-origin requests are proxied to the backend by
# Vite in dev (see vite.config.ts) and by the gateway in production.
VITE_API_BASE_URL=
